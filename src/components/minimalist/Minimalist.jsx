export default function Minimalist({ className }) {
    return (
        <div className={`minimalist-view ${className}`}>
            <h3>Minimalist</h3>
            <img alt="Minimalist" />
        </div>
    );
}