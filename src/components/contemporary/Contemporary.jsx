export default function Contemporary({ className }) {
    return (
        <div className={`contemporary-view ${className}`}>
            <h3>Contemporary</h3>
            <img alt="Contemporary" />
        </div>
    );
}