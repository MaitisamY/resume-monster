export default function Standard({ className }) {
    return (
        <div className={`standard-view ${className}`}>
            <h3>Standard</h3>
            <img alt="Standard" />
        </div>
    );
}